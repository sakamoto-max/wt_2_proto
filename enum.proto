syntax = "proto3";

package enum;

option go_package = "/shared/enum";

enum ServiceName {
    ENUM_SERVICE_NAME_UNSPECIFIED = 0;
    PLAN_SERVICE = 1;
    AUTH_SERVICE = 2;
    TRACKER_SERVICE = 3;
    EXERCISE_SERVICE = 4;
    EMAIL_SERVICE = 5; 
}

enum TaskStatus {
    ENUM_TASK_STATUS_UNDEFINED = 0;
    TASK_COMPLETED = 1;
    TASK_PENDING = 2;
    TASK_NOT_COMPLETED = 3;
    TASK_FAILED = 4;  
}

enum TaskName {
  ENUM_TASK_NAME_UNDEFINED = 0;
  CREATE_EMPTY_PLAN_FOR_USER = 1;
  SEND_EMAIL_FOR_SIGNING_UP = 2;
  UPDATE_PLAN = 3;  
}

enum QueueName {
    ENUM_QUEUE_NAME_UNDEFINED = 0;
    PLAN_QUEUE = 1;
    RESULT_QUEUE = 2;
    EMAIL_QUEUE = 3;
}

